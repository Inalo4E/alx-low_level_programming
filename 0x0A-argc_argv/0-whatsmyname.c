#include <stdio.h>

/**
 * main - print filename.
 * @argc: number of arguments entered in command line.
 * @argv: holds the string value of arguments entered.
 *
 * Return: zero
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	argc = argc;

	return (0);
}
